 # USERS GUIDE
 
© 2026 by Satoshi Endo / ZEN University / https://x.com/hortense667

 ## 1. 使い方の全体像
 - 講師と受講者が **同じURL** にアクセス
 - `room` を揃えることで同じ部屋に参加
 - 受講者はコードを貼り付けて **Run** を押したときだけ実行
 - 講師は受講者一覧から選択し、コードやログを確認、実行もできます
 
 ## 2. 画面の開き方
 **受講者**
 - `https://<your-domain>/?room=demo`
 
 **講師**
 - `https://<your-domain>/?role=teacher&room=demo`
 
 **QR ページ**
 - `https://<your-domain>/qr.html?room=demo`
 
 ※ `room` は任意の名前に変更できます（例: `g1` / `g2`）。
 
 ## 3. 受講者の操作
 1. 受講者URLにアクセス
 2. `ルーム` と `なまえ` を入力（任意）
 3. コードを貼り付け
 4. **Run** を押すと実行（自動実行はされません）
 5. 実行ログが右側に表示され、講師にも共有されます
 
 よく使うボタン:
 - `Run（プログラムを動かす）` : 実行
 - `Logsクリア` : ログ消去
 - `Codeクリア` : コード消去
 - `HTML→JS抽出` : HTML内の`<script>`だけ取り出してJS化
 - `HTMLでDL` : 現在のコードをHTMLとしてダウンロード（ファイル名は日時でユニーク）。エクスプローラ（Windowsの場合）などでこのファイルをクリックすると作ったコードをいつでも動かすことができます。また、どの時点のコードか分かるようにファイル名を書き換えて保存しておけば、あとで保存したファイルの中身をコードとして貼りつけ「HTML→JS抽出」を押すことで、この時点のコードに戻すことができます。
 - `?` : 使い方（このファイル）を表示

ヘッダの通知ボタン（講師へ状態を送る）:
- `できました` : 「作業完了」を講師に通知（もう一度押すとOFF）
- `質問あり` : 「質問あり」を講師に通知（もう一度押すとOFF）

ヘッダの関係資料:
- `関係資料` : 講師が用意したリンク集を開きます（リンクは別タブで開きます）

講師からのメッセージ:
- 受講者ヘッダに、講師からのメッセージが表示されます（講師が送信した内容）
 
無限ループで固まったとき（受講者）:
- JavaScriptの無限ループ（例: `while(true)`）は、ブラウザ（タブ）自体を固めることがあります。原則として「停止ボタン」では止められません。
- 軽い固まりなら **更新（リロード）** で復帰できることがあります
- 操作自体が効かない場合は **タブを閉じる**（最悪ブラウザを終了）してください
- 復帰後、必要なら
  - `HTMLでDL` していたファイルから復元する（または手元のコードを貼り直す）
  - 講師に依頼して `Code書き戻し` で差し替えてもらう

 ## 4. 講師の操作
 1. 講師URLにアクセス
 2. 受講者一覧から対象をクリック
 3. コード/ログを確認
 4. `別タブで実行（固まり対策）` で講師が実行（投影向け）

受講者一覧の見方:
- 行の左側（名前/経過秒）をクリックすると、その受講者を選択してコード/ログを表示します
- 行の右側に `できました` / `質問あり` の状態が表示されます（ON/OFFが色で分かります）
- 各受講者に対して、同じ行の `メモ` 欄に講師だけがメモを書けます（例: 欠席、PCトラブル、質問内容）

ヘッダの操作（講師）:
- `受講者へメッセージ` : 入力して送信すると、受講者ヘッダに表示されます（全員に配信）
- `関係資料` : リンク集を表示します
  - `編集する` から、タイトルとURL（http/https）を追加・削除・保存できます（同じroomの全員に即反映）
 
 講師の注意点:
 - 重いコードや無限ループは **講師PCが固まる可能性** があります
 - `Logs: 全員（OFFで選択者のみ）` を切り替えるとログの表示範囲を調整できます
 - `コードをコピー` / `HTMLでDL` で配布用に取得できます
 - `受講者一覧リセット` で、そのroomの受講者一覧（コード/ログ/メモ/状態）を削除できます
 
 ## 5. QR ページの使い方
 - `Room` を設定して **更新**
 - 受講者用/講師用のQRとURLが表示されます
 - 教室の投影やスマホ配布に便利です
 
 ## 6. Railway に関する注意
 - Railway は **各自で契約して利用** してください
 - アカウント作成・課金・運用は **自己責任** です

## 7. ローカルで動かす場合（講師向け）
このリポジトリは Node.js サーバが必要です。`package.json` は **Railway 用（デフォルト）** ですが、ローカル利用向けに `package.local.json` のサンプルも同梱しています。

PowerShell 例（ローカル用に入れ替え）:

```powershell
Copy-Item .\package.local.json .\package.json
npm install
npm run dev
```

ブラウザで以下へアクセス:
- 受講者: `http://localhost:8787/?room=demo`
- 講師: `http://localhost:8787/?role=teacher&room=demo`
- QR: `http://localhost:8787/qr.html?room=demo`

Railway 用に戻す（PowerShell 例）:

```powershell
Copy-Item .\package.railway.json .\package.json
```
