 # USERS GUIDE
 
 ## 1. 使い方の全体像
 - 講師と受講者が **同じURL** にアクセス
 - `room` を揃えることで同じ部屋に参加
 - 受講者はコードを貼り付けて **Run** を押したときだけ実行
 - 講師は受講者一覧から選択し、コードやログを確認、実行もできます
 
 ## 2. 画面の開き方
 **受講者**
 - `https://<your-domain>/?room=demo`
 
 **講師**
 - `https://<your-domain>/?role=teacher&room=demo`
 
 **QR ページ**
 - `https://<your-domain>/qr.html?room=demo`（受講者/作品一覧のQR）
 
 ※ `room` は任意の名前に変更できます（例: `g1` / `g2`）。
 
 ## 3. 受講者の操作
 1. 受講者URLにアクセス
 2. `ルーム` と `なまえ` を入力（任意：ただし使用できる文字が制限されます。）
 3. **Connect** 後、`なまえ` の**左**（`ルーム` の右）に **出席番号**（例: 「出席番号 3」）が出ます。講師の受講者一覧の **上から同じ並びの番号** です。ルーム違いの確認や「3番の〇〇くん」のような声かけに使えます。
 4. コードを貼り付け
 5. **Run** を押すと実行（自動実行はされません）
 6. 実行ログが右側に表示され、講師にも共有されます
 
 よく使うボタン:
 - `Run（プログラムを動かす）` : 実行
 - `Logsクリア` : ログ消去
 - `Codeクリア` : コード消去
 - `HTML→JS抽出` : HTML内の`<script>`だけ取り出してJS化
 - `HTMLでDL` : 現在のコードをHTMLとしてダウンロード（ファイル名は日時でユニーク）。ファイルを開くとそのまま実行できます。
 - `HTML読み込み` : 保存したHTMLを読み込み、コードとプログラム名を復元
 - `?` : 使い方（このファイル）を表示
 
 ヘッダの通知ボタン（講師へ状態を送る）:
 - `できました` : 「作業完了」を講師に通知（もう一度押すとOFF）
 - `質問あり` : 「質問あり」を講師に通知（もう一度押すとOFF）
 
 ヘッダのURL1・URL2:
 - `URL1` / `URL2` : 講師がその席（受講者）用に設定したURLを別タブで開きます。講師が未設定のときはボタンは無効になります。
 
 ヘッダの関係資料:
 - `関係資料` : 講師が用意したリンク集を開きます（リンクは別タブで開きます）
 
 講師からのメッセージ:
 - 受講者ヘッダに、講師からのメッセージが表示されます（講師が送信した内容）
 
 無限ループで固まったとき（受講者）:
 - JavaScriptの無限ループ（例: `while(true)`）は、ブラウザ（タブ）自体を固めることがあります。原則として「停止ボタン」では止められません。
 - 軽い固まりなら **更新（リロード）** で復帰できることがあります
 - 操作自体が効かない場合は **タブを閉じる**（最悪ブラウザを終了）してください
 - 復帰後、必要なら
   - `HTMLでDL` していたファイルから復元する（または手元のコードを貼り直す）
   - 講師に依頼して `Code書き戻し` で差し替えてもらう
 
 ## 4. 講師の操作
 1. 講師URLにアクセス
 2. 受講者一覧から対象をクリック
 3. コード/ログを確認
 4. `別タブで実行（固まり対策）` で講師が実行（投影向け）
 
 受講者一覧の見方:
 - 一覧の直前に、左端の数字が受講者ヘッダの **出席番号** と同じ旨の説明が常に表示されます。各行の**名前の左**は **数字のみ**（1始まり・席の並びどおり）で、受講者画面の出席番号と一致します（空席も番号は飛ばしません）。
 - 行の左側（番号・名前/経過秒）をクリックすると、その受講者を選択してコード/ログを表示します
 - 同じ行に `できました` / `質問あり` の状態が表示されます（ON/OFFが色で分かります）
 - 各受講者に対して、同じ行の `メモ` 欄に講師だけがメモを書けます（例: 欠席、PCトラブル、質問内容）
 - 各席ごとに **URL1** と **URL2** を設定できます。入力欄に http/https のURLを入れると、その席の受講者画面の「URL1」「URL2」ボタンでそのURLを開けます。空欄のままにすると受講者側のボタンは無効です。入力欄横の「開く」で講師がプレビューできます。
 
 ヘッダの操作（講師）:
 - `受講者へメッセージ` : 入力して送信すると、受講者ヘッダに表示されます（全員に配信）
 - `URLリスト登録` : CSV（1列目URL1、2列目URL2、1行目はヘッダ可）をアップロードすると、席順にURL1/URL2を一括設定できます
 - `URL1クリア` / `URL2クリア` : 全席のURL1またはURL2を一括で空にします
 - `関係資料` : リンク集を表示します
   - `編集する` から、タイトルとURL（http/https）を追加・削除・保存できます（同じroomの全員に即反映）
 
 講師の注意点:
 - 重いコードや無限ループは **講師PCが固まる可能性** があります
 - `Logs: 全員（OFFで選択者のみ）` を切り替えるとログの表示範囲を調整できます
 - `コードをコピー` / `HTMLでDL` で配布用に取得できます
 - `HTML読み込み` で保存済みHTMLからコードを復元できます
 - `受講者一覧リセット` : その room を**受講者まわりでは白紙**に近い状態に戻します（詳細はこのファイルの **「6.」** を参照）。
 
 ## 5. QR ページの使い方
 - `Room` を設定して **更新**
 - 受講者用のQRと、作品一覧ページのQRが表示されます
 - 教室の投影やスマホ配布に便利です
 
 ## 6. サーバー側の受講者データと席（保持・自動整理・リセット）
 
 ### 6.1 受講者一覧リセット（講師ボタン）
 次のとおり **その room 内だけ** が対象です。
 
 **削除・空にするもの**
 - **全受講者**のサーバー保持データ（表示名・コード・ログ・「できました/質問あり」などの状態、プログラム名など）
 - **全席**について、受講者の割り当てに相当する情報、および **メモ・URL1・URL2**  
   - 受講者がまだいない席にだけ講師が入れていたメモ・URLも **含めて空** になります（席の**個数**は変えません）
 - 受講者の**名前変更ロック**（講師がかけたもの）は **解除** されます
 
 **消さないもの**
 - **関係資料**のリンク集
 - **講師からのメッセージ**（受講者ヘッダに出る文）
 - ブラウザの URL や講師画面の「人数」など、クライアント側の入力そのもの
 
 実行後、講師画面は空の一覧と空の席表示に揃います（サーバーから `snapshot` と席状態が送られます）。
 
 ### 6.2 自動削除（非アクティブな受講者）
 - サーバーは、**しばらくアクティブでない受講者**を一定時間経過後に自動で削除します（既定は **約 30 日（およそ 1 ヶ月）**。翌日以降も同じ端末で続きからやりたい用途向け。運用で変える場合は `README.MD` の環境変数を参照）。
 - 削除された受講者の **コードやログもサーバー上では消えます**（再参加時は、その時点の `join` で送った名前などから新しく載ります）。
 - あわせて、**座席にだけ残っていた無効な割り当て（いわゆるゴースト席）** はサーバーが整理し、その席の **メモ・URL1・URL2 も空** にします。  
   起動時に保存状態を読み込んだとき、および定期的な整理（既定では **約 60 秒ごと**）でも同様です。
 
 ### 6.3 講師一覧の「受講者1」などの表示
 - 講師の受講者一覧で、名前の代わりに **「受講者1」** のように **席番号ベースのラベル** が出ることがあります。これは **「その席には誰かがいる扱いだが、表示用の名前がサーバー側の状態と一致していない」** ときの画面上のフォールバックです。
 - **6.2** の整理や **6.1** のリセット後は、通常このずれは起きにくくなります。
 
 ## 7. Railway、fly.io、render に関する注意
 - Railway、fly.io、renderは **各自で契約して利用** してください
 - アカウント作成・課金・運用は **自己責任** です
